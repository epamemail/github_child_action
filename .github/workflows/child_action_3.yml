name: Child Action 3

on: [workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Hello, Child Action!

      - name: Event Information
        run: |
          echo "Event from '${{ github.event.client_payload.repository }}'"
